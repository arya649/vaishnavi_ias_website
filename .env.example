NEXT_PUBLIC_SITE_SLUG=vaishnavi-ias
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-or-publishable-key

# Only needed to run `npm run seed` locally. Never used at runtime, never
# sent to the browser. Get it from Supabase Dashboard -> Project Settings -> API.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
